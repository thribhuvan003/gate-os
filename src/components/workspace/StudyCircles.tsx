"use client";

import { Clock3, Heart, Link, Plus, UsersRound } from "lucide-react";
import { useState, type FormEvent } from "react";
import type { StudyCircle } from "./types";

export interface StudyCirclesProps {
  circles: StudyCircle[];
  onInvite?: (circle: StudyCircle, email: string) => void;
  onJoinSession?: (circle: StudyCircle) => void;
  onReact?: (circle: StudyCircle) => void;
}

export function StudyCircles({ circles, onInvite, onJoinSession, onReact }: StudyCirclesProps) {
  const [selectedCircleId, setSelectedCircleId] = useState(circles[0]?.id ?? "");
  const [inviteEmail, setInviteEmail] = useState("");
  const [copied, setCopied] = useState(false);
  const selectedCircle = circles.find((circle) => circle.id === selectedCircleId) ?? circles[0];
  const invite = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!selectedCircle || !inviteEmail.trim()) return; onInvite?.(selectedCircle, inviteEmail.trim()); setInviteEmail(""); };
  const copyInvite = async () => { if (!selectedCircle) return; await navigator.clipboard?.writeText(`${window.location.origin}/circles/${selectedCircle.id}`); setCopied(true); window.setTimeout(() => setCopied(false), 1500); };
  return <section className="workspace-page study-circles" aria-labelledby="circles-title"><header className="workspace-page-header"><div><p className="workspace-eyebrow"><UsersRound aria-hidden="true" /> Study circles</p><h1 id="circles-title">Study beside people, without losing your space.</h1><p>Circles only share a session goal, live presence, and a short check-in.</p></div></header><div className="circles-layout"><nav className="circle-list" aria-label="Your study circles">{circles.map((circle) => <button className={`circle-list-item${circle.id === selectedCircle?.id ? " is-selected" : ""}`} type="button" key={circle.id} aria-current={circle.id === selectedCircle?.id ? "true" : undefined} onClick={() => setSelectedCircleId(circle.id)}><strong>{circle.name}</strong><small>{circle.members.length} member{circle.members.length === 1 ? "" : "s"}</small>{circle.activeSession ? <span className="workspace-status-dot">Live</span> : null}</button>)}</nav>{selectedCircle ? <section className="circle-detail" aria-labelledby="circle-name"><header><div><p className="workspace-eyebrow">Private circle</p><h2 id="circle-name">{selectedCircle.name}</h2><p>{selectedCircle.goal}</p></div><button className="workspace-secondary-button" type="button" onClick={copyInvite}><Link aria-hidden="true" /> {copied ? "Invite link copied" : "Copy invite link"}</button></header>{selectedCircle.activeSession ? <article className="circle-live-session"><div><span className="workspace-status-dot">Live now</span><h3>{selectedCircle.activeSession.title}</h3><p>Ends at {selectedCircle.activeSession.endsAt}</p></div><div className="workspace-row-actions"><button className="workspace-secondary-button" type="button" onClick={() => onReact?.(selectedCircle)}><Heart aria-hidden="true" /> {selectedCircle.activeSession.reactions ?? 0}</button><button className="workspace-primary-button" type="button" onClick={() => onJoinSession?.(selectedCircle)}><Clock3 aria-hidden="true" /> Join session</button></div></article> : <p className="workspace-empty-state">There is no shared focus session right now. Your personal work remains private.</p>}<section className="circle-members" aria-labelledby="members-title"><h3 id="members-title">Members</h3><ul>{selectedCircle.members.map((member) => <li key={member.id}><span className={`workspace-avatar workspace-presence-${member.status ?? "offline"}`} aria-hidden="true">{member.name.slice(0, 1).toUpperCase()}</span><span>{member.name}<small>{member.status ?? "offline"}</small></span></li>)}</ul></section><form className="workspace-inline-form" onSubmit={invite}><label className="sr-only" htmlFor="circle-invite-email">Invite by email</label><input id="circle-invite-email" type="email" value={inviteEmail} onChange={(event) => setInviteEmail(event.target.value)} placeholder="Invite by email" /><button className="workspace-secondary-button" type="submit"><Plus aria-hidden="true" /> Invite</button></form><p className="workspace-field-help">Invitations are private and should expire on the server. Notes, PDFs, goals, and individual progress are never shared.</p></section> : <p className="workspace-empty-state">Create or join a private study circle to focus together.</p>}</div></section>;
}
